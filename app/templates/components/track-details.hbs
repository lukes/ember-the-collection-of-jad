<span class="position">
  {{track.position}}
</span>

{{#if trackIsPlaying}}
  <span class="pause"></span>
{{else}}
  <span class="play"></span>
{{/if}}

<span class="title">
  {{track.title}}
  {{#if track.duration}}
    <span class="duration">
      ({{track.duration}})
    </span>
  {{/if}}
</span>
